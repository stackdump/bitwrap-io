# Codex project guidance

Read `CLAUDE.md` in this repository if present before making changes. It contains
the project's conventions, architecture, and verification commands.

When this checkout is under `~/Workspace`, read `../CLAUDE.md` for the shared
project map, dependencies, and deployment rules. Use the sections relevant to
the task.

Build and test locally. Review the deployment guidance before pushing to
`main` or changing remote services; a push can deploy a served app to production.
